טיפ קטן להמשך:
בכל פעם שתעשה שינוי בקוד ב-VS Code (למשל, תוסיף עוד נתונים מה-POH או תשפר את הגרפיקה של מעטפת הטיסה), כל מה שתצטרך לעשות זה:

git add .

git commit -m "added new features"

git push 
והאתר יתעדכן מעצמו תוך כמה שניות!